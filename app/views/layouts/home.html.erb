<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="">
  <meta name="author" content="">

  <title>Retail Mapper</title>
  <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  <%= content_for(:addon_css) %>
  <%= javascript_include_tag "application" %>
  <%= content_for(:addon_js) %>
</head>

<body>

  <%= render 'layouts/header' %>

  <div class="container">
    <header role="banner" id="subnav-header">
      <nav class="navbar">
        <div class="container">
          <div class="navbar-header">
            <!-- skip the collapsable button -->
          </div>
          <div id="logo" class="title hidden-xs">
            <%= image_tag 'logo.jpg', class: 'img_logo' %>
            Retail Mapper
          </div>
          <!-- Collect the nav links, forms and other content for toggling -->
          <% if agent_signed_in? || admin_signed_in? %>
            <ul class="nav navbar-nav">
              <li class="dropdown">
                <a href="#" class="dropdown-toggle submenu_dropdown" data-toggle="dropdown">Company</a>
                  <ul class="dropdown-menu subnav-head-menu">
                    <li><%= link_to 'Agents', agents_path %></li>
                    <li><%= link_to 'Assets', assets_path %></li>
                    <li><%= link_to 'Visits', visits_path %></li>
                    <% if company_user_signed_in? && current_agent.company_id %>
                      <li>
                        <%= link_to 'Reports', reports_path(id: current_agent.company_id) %>
                      </li>
                    <% end %>
                  </ul>
              </li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle submenu_dropdown" data-toggle="dropdown">Marketing</a>
                <ul class="dropdown-menu subnav-head-menu">
                  <li><%= link_to 'White Paper', static_white_paper_path %></li>
                  <li><%= link_to 'Videos', static_videos_path %></li>
                </ul>
              </li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle submenu_dropdown" data-toggle="dropdown">Reports</a>
                <ul class="dropdown-menu subnav-head-menu">
                  <li><%= link_to 'Activity Log', '#'%></li>
                </ul>
              </li>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle submenu_dropdown" data-toggle="dropdown">Tutorials</a>
                <ul class="dropdown-menu subnav-head-menu">
                  <li><%= link_to 'Mobile App', static_mobileapp_path %></li>
                </ul>
              </li>
            </ul>
          <% end %>
        </div>
      </nav>
    </header>
  </div>

  <%= yield %>
  <!-- /.container -->

  <!-- <div class="container"> -->
    <hr>

  <%= render 'layouts/footer' %>
  <!-- </div> -->

</body>

</html>
